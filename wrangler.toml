name = "yfg"                                      # ← 必须改成这个，和你的 Worker 名称一致
main = "_worker.js"
compatibility_date = "2026-04-12"
keep_vars = true

# ==================== KV 永久绑定（已修正 ID）====================
kv_namespaces = [
  { binding = "KV", id = "cfe75899082d42debc10de21e0f3bc7f" }
]

# ==================== 推荐一起加上（避免下次再出现配置冲突）====================
[[routes]]
pattern = "yfg.gyf111.dpdns.org/*"
zone_name = "dpdns.org"

[vars]
ADMIN = "a.123456"
